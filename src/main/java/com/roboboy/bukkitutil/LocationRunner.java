package com.roboboy.bukkitutil;

import org.bukkit.Location;

public interface LocationRunner {
	
	/**
	 * Runs at the given location.
	 * @param location Copy of location to run at
	 */
	public void runAtLocation(Location location);

}
